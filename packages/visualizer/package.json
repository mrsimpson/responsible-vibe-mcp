{
  "name": "@codemcp/workflows-visualizer",
  "version": "4.7.0",
  "type": "module",
  "main": "dist/index.ts",
  "module": "dist/index.ts",
  "exports": {
    ".": "./dist/index.ts",
    "./dist/*": "./dist/*"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node ../../scripts/generate-workflow-list.js && mkdir -p dist && cp -r src/* dist/",
    "dev": "npm run build && nodemon --watch src --ext ts,vue,js --exec 'npm run build'",
    "clean:build": "rimraf ./dist",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "d3": "^7.9.0",
    "js-yaml": "^4.1.0",
    "marked": "^16.4.1",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@types/node": "^20.19.23",
    "nodemon": "^3.1.10",
    "rimraf": "^5.0.10",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "vue": "^3.4.0"
  }
}
